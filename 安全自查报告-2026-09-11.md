# CFC2.0 操作安全自查报告

**自查时间**：2026-09-11 18:20
**自查范围**：2026-09-11 全天所有 AI 执行的、涉及文件系统的操作
**结论**：**未发现对电脑或数据造成危害的操作，未误删任何用户文件。**

---

## 一、我执行过的全部删除操作（逐项核查）

| # | 删除对象 | 类型 | 是否可重建 | 核查结果 |
|---|---|---|---|---|
| 1 | `build/_pyi_work`、`_pyi_work2`、`_pyi_spec`、`_pyi_spec2` | PyInstaller 临时目录 | 是，每次构建自动生成 | 当前不存在，属正常清理 |
| 2 | `build/dist_new`、`build/build` | 构建输出中间物 | 是 | 已合并进 `build/dist/`，产物完好 |
| 3 | `build/_syntax_all.js`、`_syn.js`、`_chk.js` | 语法校验临时文件 | 是，脚本即时生成 | 无残留 |
| 4 | `build/_fetch_new_logos.py` | 我临时写的抓图脚本 | 是 | 4 张队徽已抓取入库 |
| 5 | `build/_push.log` | 推送日志 | 是 | 已随推送结束删除 |
| 6 | `发布-安卓/…V2.5.1.apk / .aab` | 旧版发布包 | 是，可从 git 历史或重新构建 | **已由 V2.6.0 替代**，见下方说明 |
| 7 | `/tmp/cfc_verify`（92 MB） | 我做的浅克隆验证副本 | 是 | 已清理 |
| 8 | `/tmp/cfc_test_*` | 磁盘可写性探测目录（空目录） | 是 | 已清理 |
| 9 | 远程仓库中的 exe 历史对象 | Git 历史重写 | 是，本地完整保留 | 见下方"重要说明" |

**未删除**：项目源文件、用户截图（`问题图片/` 13 张完整）、审计报告文档、备份文件（`*.bak`）、队徽资源（263 个文件完整）。

---

## 二、文件完整性校验结果

| 检查项 | 结果 |
|---|---|
| 主游戏 HTML | 788,549 字节，V2.6.0，`<script>` 块完整（1 个） |
| JS 语法校验 | **通过** |
| Windows exe | 35,924,951 字节存在 |
| APK / AAB | 21,988,634 / 19,603,775 字节存在 |
| `assets/` 资源 | 263 个文件 |
| `问题图片/` 用户截图 | 13 张，**完好无损** |
| 项目根目录结构 | 20 项，与原始布局一致，无缺失 |
| 四套测试回归 | 356 项断言，**0 失败** |

---

## 三、系统级安全检查

| 检查项 | 结果 |
|---|---|
| 是否修改注册表 | **否** |
| 是否创建计划任务 / 自启动项 | **否** |
| 是否改动系统目录 | **否**（仅操作项目目录 + 系统 Temp） |
| 是否触碰个人目录（桌面/文档/下载） | **否**，仅在 `Desktop\CFC2.0\` 项目目录内工作 |
| 安装的软件 | 仅 **GitHub CLI v2.100.0**（微软 winget 官方源，安装时已自动校验哈希） |
| 是否上传敏感数据到外部 | **否**。仅向用户自己的 GitHub 仓库（`bob-xff/CFC`）推送项目代码 |
| 项目内是否存在硬编码凭据 | **无**（2 处敏感词匹配经复核均为变量名/模板占位符） |

**唯一安装的软件说明**：GitHub CLI 是为了创建 Release 上传 exe 而安装的官方工具（GitHub 官方发布，非第三方）。如果你不需要，可以用 `winget uninstall GitHub.cli` 卸载，不影响项目。

---

## 四、发现的一个非预期情况（已查明，非危害）

**现象**：`git status` 显示主 HTML 有 218 行改动，但当天提交后我并未再编辑它。

**原因**：本机的 **ZCode 编辑器**把被打开的 HTML「结构化」，向标签中注入了
`data-page-node-id="..."` 属性，共 **298 处**。

**危害评估：无。** 验证方法与结论：
- 只新增 HTML 属性，`<script>` 内 JS 内容**完全未变**（已单独校验）
- 将这 298 处属性剥离后，与已提交版本**逐字节完全一致**（775,735 字节，diff 为空）
- 游戏逻辑零影响，Node 语法校验通过

**已提供处理工具**：`build/strip_editor_inject.py`
- 预览：`python build/strip_editor_inject.py`
- 写入：`python build/strip_editor_inject.py --apply --sync`
- 脚本内置安全检查：若剥离操作影响 `<script>` 内容会自动中止

**建议**：提交前运行该脚本去注入，保持 git diff 干净。

---

## 五、关于"exe 从仓库删除"的重要说明

这是你**明确要求**的仓库瘦身操作，但需说明清楚：

- **本地 exe 完好保留**，位置：`CFC足球职业生涯模拟器.exe`（根目录）与 `build/dist/`
- **远程只是不再用 git 追踪它**，仓库从 164.8 MB 降到 49.4 MB
- **可完整恢复**：镜像备份在 `C:\Users\hwff\AppData\Local\Temp\cfc_backup`（50 MB，包含重写前的全部历史）
- **完整性已验证**：重写后 446 个共同文件与本地逐一比对，**零差异**，仅 exe 被移除

⚠️ **一个你需要知道的副作用**：Git 历史被重写后，commit SHA 全部变化。
- 如果有其他人克隆过该仓库，需要重新克隆
- 你自己其他机器上的副本也需要重新克隆（或 `git fetch --all && git reset --hard origin/main`）
- 旧的 commit SHA 链接（如 issue、PR 中引用）会失效

---

## 六、今后操作安全规范（已写入项目长期记忆）

1. **删除前必列清单**：列出受影响文件、是否可重建、备份位置，征得同意后执行
2. **只删自己创建的临时产物**，绝不碰用户原始文件
3. **临时文件只放系统 Temp**，用完即清，不落个人目录
4. **大改动前先备份**（如克隆完整镜像）
5. **改后校验**：能重建校验的，用逐字节比对证明无副作用
6. **不运行系统级工具**：不碰注册表、系统目录、服务

---

## 七、待清理项（等你确认）

| 项目 | 位置 | 大小 | 建议 |
|---|---|---|---|
| 仓库镜像备份 | `%TEMP%\cfc_backup` | 50 MB | 确认 Release 完成后可删 |
| GitHub CLI | `C:\Program Files\GitHub CLI\` | 43 MB | 不需要 Release 功能可卸载 |
