# CFC 足球职业生涯模拟器 V2.2.0 — 安卓版（移动端）实施与审计报告

日期：2026-09-07
范围：`mobile/`（Capacitor 7 安卓工程）→ `football-career-simulator.html`（移动端适配）→
签名 APK/AAB → 模拟器（Pixel 4 · API 35）实测验收
目标产物：**CFC足球生涯（测试版）**（com.cfc.footballsim），横屏、商店预备、浏览器版完整保留

---

## 一、总体架构（一套代码，两个出口）

- `football-career-simulator.html` 仍是**唯一源文件**：浏览器版行为保持不变，
  安卓版由 `mobile/sync.py` 把它复制为 `mobile/www/index.html` 并随队徽资源进入原生壳。
- 原生桥全部通过 `window.Capacitor` 存在性判断（`CAP/CAP_PREFS`），浏览器环境中恒为 null，
  JS 主体零侵入；共三个接入点：存储双写、返回键、全屏状态栏。
- `mobile/` 工程已入库（android/ 原生工程随库提交，构建产物按 .gitignore 排除）。

## 二、环境决策与踩坑记录（本机实际发生）

1. **无需安装 Node/JDK**：Node v24.15.0、Oracle JDK 17.0.18 本机已有，未重装。
2. **未装 Android Studio**：构建只需命令行 SDK（装至 `D:\Android\Sdk`：
   platform-tools / platforms;android-35 / build-tools;35.0.0 / emulator / API35 x86_64 镜像）。
3. **JDK 21**：Capacitor 7 的 `capacitor-android` 模块固定 `VERSION_21` 字节码，
   JDK 17 报"无效的源发行版：21"。已在 `mobile/android/gradle.properties` 写入
   `org.gradle.java.home=D:/Android/jdk-21.0.12.1`（Temurin→Oracle 21.0.12.1，装于 D 盘），
   **不影响系统其它项目**。
4. **Gradle 发行版下载证书失败**：`services.gradle.org` 在本机网络下 TLS 验证失败
   （JDK 侧 PKIX 报错，keytool 探测无输出）。改用腾讯镜像
   （gradle-wrapper.properties → `mirrors.cloud.tencent.com/gradle/gradle-8.11.1-all.zip`）后正常。
   dl.google.com / mavenCentral 验证正常，无需镜像。
5. **local.properties**：`sdk.dir` 反斜杠转义书写错误会导致
   "文件名、目录名或卷标语法不正确"，已改为正斜杠 `D:/Android/Sdk`（不入库，sync 后自动生成逻辑见下）。
6. **@capacitor/assets 放弃**：其 sharp 原生依赖在当前网络证书环境下装不上；
   改用 Pillow 手写生成全套图标与启动屏（`mobile/tools/generate_res.py`），零外部依赖。
7. **Edge 自动化失败**：本机 Edge 152 无法被 puppeteer 驱动（无 stderr 退出）。
   桌面截图台改用 Chrome for Testing；后模拟器实测覆盖，未再依赖。
8. **windowLayoutInDisplayCutoutMode**：该属性需 API 27+ 且拼写为 `InDisplay`
   （首试两处均错导致 aapt 链接失败），已放入 `res/values-v27/styles.xml`。

## 三、移动端适配明细（M1 触摸 + 屏幕）

1. **hover 治理**：31 条元素 `:hover` 规则全部包进 `@media(hover:hover)`
   （脚本 `mobile/tools/wrap_hover.py` 一次性转换，滚动条伪元素 2 条除外）。
   触屏点击后不再出现 hover 态"粘滞"。
2. **按压反馈**：全部按钮新增 `:active` 缩放（0.97）/位移反馈，
   触屏与鼠标通用；`touch-action:manipulation` 消除双击缩放与点击延迟；
   `-webkit-tap-highlight-color` 置透明；导航/顶栏/芯片禁用长按选中。
3. **横屏紧凑布局**（`@media (orientation:landscape) and (max-height:540px)`）：
   - 左侧导航压缩为 62px 纯图标竖排（复位 ≤1024px 的单列横向导航），
     5 个视图（剧情/成长/数据/荣誉/履历）对应 5 个图标按钮，触点高约 52px；
   - 顶栏 7px 紧凑内边距、队徽 38px、芯片缩号；窄横屏（≤700px 宽）隐藏副标题防溢出；
   - 舞台/横幅/卡片密度收紧，弹窗 `max-height:calc(100dvh - 16px)` 内滚；
   - `#game-screen` 高度 `100dvh`（含 fallback），规避 WebView 视口偏差。
4. **全面屏安全区**：`viewport-fit=cover` + `env(safe-area-inset-*)`
   用于游戏屏网格与各页面/弹窗左右内边距；`windowFullscreen=true` 全屏沉浸
   （状态栏隐藏，手势条保留，已实测）；values-v27 配置 `shortEdges` 切口延伸。
5. **横屏锁定**：Manifest `screenOrientation="sensorLandscape"`（随重力 180° 翻转）。
6. **画面不减配**：所有渐变、发光、动画（fadeIn/过渡）、FUT 卡片、SVG 线性图标、
   双主题全部保留；模拟器截图逐页比对无缺失。

## 四、存档存储（M2）

- 现状：3 个本地槽 `fcs_save_0..2` + `fcs_theme`，共 6 处 localStorage 调用。
- 方案：**双写 + 启动恢复**（非异步化改造，游戏逻辑零改动）：
  - 写入：`saveGame/applyTheme` 在 localStorage 后同步镜像写 Capacitor Preferences（原生 SharedPreferences）；
  - 删除：`deleteSave` 同步双删；
  - 恢复：App 启动 `restoreFromNativeStore()`——localStorage 为空而 Preferences 有数据时自动回填
    （防 WebView 存储被系统回收），主题同理。
- 实测证据：模拟器存档后 `shared_prefs/CapacitorStorage.xml` 中 `fcs_save_1`
  含完整游戏 JSON（TestPlayer/ST/上海海港…）✓；游戏内 toast「已保存到存档 1」✓。

## 五、返回键（M2）

优先级链与 ESC 键一致：确认弹窗 → 游戏菜单 → 结算窗（刻意不响应，防误触跳过）→
游戏内非剧情页回剧情页 → **2.2 秒内双击退出**（否则 toast「再按一次返回键退出」）。
实测：数据页按返回 → 自动回剧情页 ✓；剧情页按返回 → 退出提示 toast ✓。

## 六、资源优化（M3，画质承诺：显示尺寸下与原图一致）

- 53 张超 100KB 队徽（最大 676KB/512px）统一缩至最长边 256px（显示上限 104px 的 2.5 倍）
  + 256 色调色板 + Floyd-Steinberg 抖动：**8.9MB → 0.66MB**；总资产 13.7MB → 约 5.4MB。
- 输出至 `mobile/assets_opt/`（镜像目录结构，sync 时覆盖拷贝；原始 assets/ 不动，浏览器版同步受益）。
- 抽检（赖扬队徽 512px→256px）与模拟器实机页头/赛季横幅渲染：无可见差异 ✓。
- 中文文件名（乌姆沙拉尔.png 等）UTF-8 全链路正常 ✓。

## 七、应用图标 / 启动屏 / 名称（M3）

- `generate_res.py`：icon_1024.png → 传统方形/圆形图标（48~192px 全密度）+
  自适应前景（62% 安全区）；`ic_launcher_background` 由白改为 **#05070C**；
  启动屏（横竖全密度 + drawable 默认）= #05070C 深底 + 启动页 logo 居中。
- 应用名：`CFC足球生涯（测试版）`（strings.xml，桌面/任务栏一致）；
  appId `com.cfc.footballsim`；versionCode 220 / versionName 2.2.0；
  HTML 内 `GAME_VERSION` 同步升 **V2.2.0**（单一版本源机制沿用）。
- 实测：桌面图标圆形深色 CFC 徽标 ✓；启动屏 → 游戏首屏衔接 ✓。

## 八、构建与签名（M4）

- 签名：`cfc-release.keystore`（RSA 2048，有效期 30 年，CN=CFC Football Simulator）。
  **密码：`Z0fUH1kyRx4GkvolT4Ev`**；配置在 `mobile/android/keystore.properties`（不入库）。
  **已双备份：`mobile/android/app/cfc-release.keystore`（本机）+ `D:\Android\keystore-backup\`
  （含密码文件）。⚠️ 此密钥是商店后续更新的唯一凭证，请务必自行再备份一份（网盘/密码管理器）。**
- 产物（`发布-安卓/`，不入库）：
  - `CFC足球生涯（测试版）-V2.2.0.apk`：11.4MB（11,996,136B），apksigner 验证
    签名者 CN=CFC Football Simulator ✓ —— 直接分发（微信/QQ/网盘）用
  - `CFC足球生涯（测试版）-V2.2.0.aab`：10.3MB —— Google Play 等商店提审用
- 商店预备项已就绪：AAB、图标、隐私权限最小化（仅 INTERNET，且 WebView 全部本地资源、无网络请求）。

## 九、模拟器实测清单（M5，Pixel 4 · API 35 · x86_64）

| # | 项目 | 结果 |
|---|---|---|
| 1 | 安装启动（debug + release 双包） | ✅ |
| 2 | 横屏锁定 + 全屏沉浸（无状态栏） | ✅ |
| 3 | 版本徽章 V2.2.0 | ✅ |
| 4 | 开始页/创建页/存档页布局与滚动 | ✅ |
| 5 | 输入法唤起（adjustResize 下表单可滚动、姓名可输入） | ✅ |
| 6 | 开档→剧情页（2026赛季开始横幅、队徽/联赛标志渲染） | ✅ |
| 7 | 成长页 FUT 卡片 / 数据页三列网格 | ✅ |
| 8 | 菜单弹窗（保存/读取/管理/主题/退出全项可见） | ✅ |
| 9 | 手动存档 toast + Preferences 双写落盘 | ✅ |
| 10 | 返回键：视图回退 + 双击退出提示 | ✅ |
| 11 | 浅色主题切换（toast + 全局变色） | ✅ |
| 12 | 桌面图标/名称 | ✅ |

**浏览器版回归**：`build/revamp/_smoke_test.js` 52/52 全过（改造后同跑）；
桌面布局不受移动 CSS 影响（横屏紧凑层仅命中"宽>高且高≤540"窗口）。

## 十、遗留事项与建议（下轮可做）

1. **真机回归**：模拟器已覆盖功能面，建议在有安卓真机时过一遍第五节清单
   （重点：真实刘海屏左右安全区、低端机流畅度、微信内直接安装拦截提示）。
2. **上架材料**：国内商店需软件著作权证书 + 备案；Google Play 需开发者账号（一次性 $25）
   + 数据安全表单（本游戏无收集、无网络请求，填"不收集"即可）。
3. **存档导出**（未做）：可加"导出/分享存档文件"，方便换机；本期未纳入。
4. `gradle-wrapper.properties` 的腾讯镜像、`org.gradle.java.home` 路径为**本机环境相关**，
   换机器构建时需对应调整（已在文件内有注释）。
5. build/revamp 零件源码已封存为历史（V2.1.1 同步校验后不再回写）；今后直接改单文件 HTML。

## 十一、发版记录

- 提交：`CFC V2.2.0（测试版）：安卓版上线（Capacitor 横屏壳 + 触摸/横屏/安全区适配 + 存档双写 + 资源瘦身）`
- 标签：`v2.2.0`；远程：github.com/bob-xff/CFC（main）
