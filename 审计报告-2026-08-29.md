# CFC 足球职业生涯模拟器 V2.0 — 问题图片审计与修复报告

日期：2026-08-30（第三轮更新，覆盖 08-30 新增 5 张截图与主题/荣誉/教练模式需求）
范围：`问题图片/` 全部 14 张截图 → 源码（`build/revamp/`）→ 组装产物 `football-career-simulator.html` → 单机 exe

---

## 〇-B、第四轮：智能应用控制拦截（exe 无法运行）——诊断与解决方案

用户再次反馈"智能应用控制已阻止"（截图路径为 `CFC2.0\CFC1.0\` 的旧版拷贝，该子文件夹现已不存在）。
本机实测诊断：
- exe **无数字签名**（Get-AuthenticodeSignature = NotSigned）、无 MOTW 标记；
- 注册表 `VerifiedAndReputablePolicyState = 1`，即**智能应用控制（SAC）处于开启状态**。

**结论**：SAC 开启时会拦截一切无签名程序——与游戏版本新旧无关，任何重打包都无法绕过，
且该弹窗没有"仍要运行"按钮。能落地的三条路径：改玩浏览器版 / 关闭 SAC / 购买代码签名证书。

已交付（根目录）：
1. **`启动游戏-浏览器版.bat`**（推荐）：自动检测 Edge 并以 `--app` 独立窗口模式打开游戏
   （无地址栏，体验与 exe 几乎一致）；无 Edge 时回退默认浏览器。实测可正常拉起，
   完全不受 SAC 影响，存档走浏览器 localStorage 自动保存。
2. **`关闭智能应用控制-管理员运行.bat`**（可选）：自提升管理员权限，写入
   `VerifiedAndReputablePolicyState = 0`（与系统设置中手动关闭等效），重启电脑后 exe 可直接双击运行；
   脚本内明确提示"SAC 一旦关闭通常需重装系统才能重新开启"，由用户自行决定是否执行（脚本默认未运行）。
3. README「如何游玩」重排为浏览器版优先，【常见问题】按"拦截机制不同"区分 SAC 与 SmartScreen
   的处理方式，并注明旧版子文件夹（CFC1.0）拷贝已过期勿用。

## 〇-A、第三轮：08-30 新增截图与整改

新增截图：`2026-08-30 194150 / 194351 / 195006 / 195034 / 195117`，对应五项整改：

### 1. 标志扭曲——真正的根因（本轮根治）
- **现象**：葡超标志（LIGA PORTUGAL，112×150 **竖版**）被拉进 120×64 横版框严重压扁（截图 195034）；CSL 字标同样变形（截图 194150）。
- **真根因**：CSS 中 `object-fit:contain` 只定义在 `.crest` 基类上，而页面大量场景**独立使用** `crest-lg/crest-md` 等尺寸类（不带基类）→ `object-fit` 从未生效，图片按默认 `fill` 拉伸。上一轮"队徽填充正方形 + 联赛宽框"只是缓解，没有触达这条规则缺口。
- **修复**：`.crest,.crest-sm,.crest-xs,.crest-md,.crest-lg,.crest-xxl` 共享规则组统一声明 `object-fit:contain` + 底板/描边（css.css 队徽体系段）。探针实测：`crest-lg crest-league` → contain、120×64 ✓；独立 `crest-md` → contain ✓。

### 2. "球队 VS 联赛"怪异对垒 → 球队 × 赛事信息条
- 赛季开始页取消"你的球队 VS 联赛"的无意义对垒（js5.js `showSeasonStart` + css.css `.season-clubline`）：
  - 左侧：俱乐部大盾徽 + 队名 + 城市；
  - 右侧：联赛标志（小宽框）+ 联赛名 + 赛程要素（轮次·杯赛·洲际）+ 本季目标（按梯队/总评动态生成）。

### 3. 浅色/深色双主题
- `html[data-theme="light"]` 覆盖全部设计令牌 + 顶栏/按钮/卡片/队徽底板/滚动条等深色特例（css.css 末尾主题段）；
- 主菜单与游戏菜单各加「切换浅色/深色主题」按钮（body.html），`toggleTheme()` 持久化到 `localStorage(fcs_theme)`，启动时 `initTheme()` 恢复；
- 浅色下 Volt 强调色自动加深为 #65a30d 保证可读性；CFC 白色标志在浅底自动获得深色底板。
- 浏览器实测：切换即时生效、刷新后保持（截图验证）。

### 4. 荣誉评判标准合理化（截图 195006：19 岁 6 球 9 助拿下"葡超最佳球员"）
- 重写 `showSeasonEnd` 荣誉阶梯（js5.js），由高到低、命中即停、产出门槛硬性化：
  | 荣誉 | 标准 |
  |------|------|
  | 最佳门将（GK） | 零封 ≥12 且评分 ≥7.0 |
  | 金靴 | 达到该联赛射手王门槛（中超16/葡超15/英超17…球） |
  | 联赛最佳球员（MVP） | 评分 ≥7.45 **且**（≥14球 或 ≥10助 或 总产出≥18）——6 球 9 助不再可能 |
  | 助攻王 | 助攻 ≥10 且多于进球 |
  | **最佳年轻球员（新增）** | U21 且评分 ≥6.95 且总产出 ≥8 —— 年轻球员的主要荣誉出口 |
  | 最佳阵容 | 评分 ≥7.2 且总产出 ≥8 |
  | 队内赛季最佳 | 评分 ≥6.8（球队级，门槛最低） |
- 中国金球奖同步收紧：评分 ≥7.4 且（≥14球 或 ≥9助 或 总产出≥16）。
- 单测验证：19 岁 6 球 9 助 → **最佳年轻球员** ✓；26 岁 6 球 9 助 → 最佳阵容 ✓；8 球 12 助 7.6 分 → 最佳球员 ✓；16 球 → 金靴（优先于 MVP）✓；2 球 3 助 → 无/队内 ✓。

### 5. 身价变化符合现实
- 新增**赛季表现系数**（js2.js `calculateMarketValue`）：结算时记录 `lastPerf`（出场/进球/助攻/零封/评分），估值按产出相对预期（场次基准）浮动 **±15% 有界**——爆发季溢价、疲软季折价，不再只随总评跳变；
- 荣誉溢价封顶：从最多 +20% 收敛为 +12%（每项 1.5%），杜绝荣誉堆叠通胀。

### 6. 2030 赛季踢"2034 世预赛"的年份错乱（截图 195117）
- **根因**：`cal_*` 日历剧情（亚洲杯/奥运/世预赛/世界杯）同时存在于章节剧情池，`pendingStoryEvents` 不按年份过滤，只要章节/年龄/旗标符合就触发。
- **修复**：`pendingStoryEvents` 跳过一切 `cal_*` 事件（js3.js），它们只走 `pickCalendarEvents` 的真实赛历年份闸门。

### 7. 教练生涯剧情完善（40 岁退役后）
- 原"转战教练"只是一个静态总结页。现改为**互动执教模式**（js5.js）：
  - **起点**按球员生涯声望定位：总评 84+/荣誉 6+ → 中超争冠球队主帅；76+/3+ → 中游球队；68+/1+ → 助理教练；其余 → 青训梯队主帅；
  - **执教阶梯**：青训梯队 → 中超助教 → 中超中游 → 中超争冠 → **国家队主教练**；声望 78+ 获得升迁邀约，<20 黯然下课（降级重开）；
  - **每赛季一个执教抉择**（6 事件池：连败质疑/更衣室矛盾/转会窗预算/核心求去/研修班/轻敌轮换），选择实时影响声望；
  - 赛季结算：声望推算中超排名，冠军记入执教荣誉（奖杯墙）；
  - 随时可"结束执教生涯"查看总结（年龄/声望/荣誉/终点站位）。

---

## 〇、第二轮：新增截图与整改需求（回顾）

新增截图 `屏幕截图 2026-08-29 215922.png`：赛季开始 VS 页中**中超（CSL）联赛标志被压扁**。
结合本轮反馈，共完成六项整改（L/G/I/H/J/K），全部验证通过。

### L. 队徽与联赛标志全面适配（第二轮方案；真根因已于第三轮根治，见上文 1）
- **根因**：162 个标志文件中 61 个非方形——50+ 支欧洲球队队徽为 139×181 竖版、15 个联赛标志为宽幅字标（如 CSL 318×150），而展示框全部是正方形，导致视觉压扁/失衡。
- **修复**：
  - `build/revamp/pad_logos.py`：51 个竖版队徽居中填充为正方形透明底画布（幂等，源文件级修复，HTML 与 exe 同时生效）；
  - 联赛字标保留原比例，`crestHTML()` 为 league 类自动附加 `crest-league` 类（js1.js:59），CSS 新增宽幅展示框体系（约 2:1，含 sm/xs/md/lg/xxl/响应式全尺寸），`object-fit:contain` 等比缩放；
  - 浏览器截图实测：海港盾徽完整方正、CSL 字标比例正常。

### G. 转会窗规范化（冬窗/夏窗、中外赛制差异）
- **原状**：每赛季只有一个无名转会窗，随机插在赛季中段——"乱七八糟"。
- **新规**（js1.js `transferWindows()` + js3.js `generateSeasonEvents()` + js5.js `showTransferWindow()`）：
  - **每赛季两个转会窗，位置固定**：
    - 中国/东亚/西亚/美洲（单自然年赛季）：**季前【冬窗】(1月1日—2月28日)** + **季中【夏窗】(6月1日—7月15日)**；
    - 欧洲（跨年赛季）：**季前【夏窗】(6月10日—9月1日)** + **季中【冬窗】(1月1日—1月31日)**；
  - 窗口 UI 标题规范为「转会窗 · 冬窗（季前 · 1月1日 — 2月28日）」格式，窗内"主动寻求转会"刷新后窗期标签保持；
  - 合同到期必须在季前窗处理（续约/转会/自由市场），逻辑与真实规则一致；
  - **季前窗完成转会 → 赛季数据按新联赛重新模拟**（此前转会后仍按旧联赛出数据）。

### I. 关键时刻可能失误（风险判定机制）
- **机制**（js3.js `processChoice`）：选择可声明 `risk:{attr,base}`——成功率 = 基础值 + 相关属性加成 + 状态/总评微调（25%~92% 之间）；失败走专门编写的 `fail` 分支：**无进球/助攻/旗标**，状态下降，文案为独立的失误叙事（被扑出/击中横梁/踢飞等）。
- **覆盖 21 处关键选择**：单刀/推射/爆射/远射/油炸丸子/长途奔袭/点球三种罚法/门将扑点/杯赛绝杀/亚冠半决赛与决赛/欧冠决赛/亚洲杯制胜球/世预赛任意球/世界杯首球与决赛挑射。
- **选择真实改变命运**：例如世预赛最后一轮选择"直接任意球"（高风险高回报 base 45%）失误 → 不置 `wc_qualified` 旗标 → 该届世界杯剧情链自然关闭；保守选择则成功率 100% 但收益低。
- **存档一致性**：成败结果写入 `season.lastRiskFail`，读档恢复时渲染同一分支（已测）；失误时结果标题显示「剧情 · 失误」。

### H. 选择影响后续（贯穿玩法）
此前大量旗标（ajax_interest、recovery_pro、weakfoot_boost 等）设置后从未被使用。现已全部接入：
- `ajax_interest`（阿贾克斯球探考察）/ `go_abroad_intent`（留洋决心）→ **转会市场真实出现荷甲/葡超报价**（ovr≥58 且效力中超/中甲时）；
- `recovery_pro`（科学康复）→ 30 岁后衰退每年 +1 缓解；`weakfoot_boost`（≤21 岁）/`mentor_technique`（≤19 岁）→ 成长期额外 +1；
- `pro_lifestyle`（自律生活）→ 每季开局状态 6 而非 5；`injured_play`（打封闭硬撑）→ 下一季开局状态 4 并写入履历"当时的选择留下了后果"；
- `viral`/`sponsor` → 身价 ×1.05；赛季结算新增「赛季回响」履历行汇总场外后果。

### J. 最迟 40 岁退役（含漏洞修复）
- **发现绕过漏洞**：原实现 `if(p.age>=40&&!p.flags.retired40)` 在首次触发后置旗标，玩家选"继续征战"后该检查**永久失效**，可以无限踢下去。
- **修复**：40 岁起 `startNewSeason` 直接进入退役流程，退役界面隐藏「继续征战」选项（body.html 加 `id="retire-continue"`），`chooseRetire` 兜底拒绝；40 岁退役为硬上限。

### K. 退出游戏真正关闭程序
- **原状**：`window.close()` 在浏览器/pywebview 中均无效，按钮形同虚设。
- **修复**：`build/cfc_game_app.py` 暴露 `js_api`（`Api.quit()` → 销毁 webview 窗口并退出进程）；`menuQuitApp` 优先调用 `window.pywebview.api.quit()`，浏览器模式回退 `window.close()` 并提示。

### 剧情漏洞清扫（附带）
- 「留洋决心」剧情后果文案与机制对齐（此前宣称"三天后完成转会"实际什么都没发生；现改为"下一窗报价上桌"，且 G/H 使其真实生效）；
- 欧洲首秀/首球剧情的"联赛第X轮"占位符 → 随机真实轮次；
- 窗内刷新保持窗期标签（seekTransfer 透传窗期）。

---

## 一、第一轮：问题图片解读与修复（回顾）

| # | 截图 | 玩家所见 | 判定 |
|---|------|----------|------|
| 1 | `da2536…` | 剧情后续文本直接显示 `"如何评价${p.name}的表现"` | **Bug A：占位符未插值** |
| 2 | `9200c4…` | 玩家效力**法甲里昂**（S5·2030，20岁），剧情却庆祝"**亚冠**历史上值得铭记的一夜" | **Bug B：亚冠事件在非亚洲联赛触发** |
| 3 | `a2309c…` | 21岁、已在里昂一线队，比赛事件却是"**青年联赛**第8轮，里昂**青年队**" | **Bug C：青年赛事文案不跟随梯队** |
| 4 | `83c85d…` | 事件地点显示**伦敦**，剧情却是阿贾克斯球探来考察、"有可能踏上欧洲足球的舞台"（玩家已留洋） | **Bug D：留洋后仍触发"留洋前"球探剧情**（地点本身跟随球队，属正常） |
| 5 | `8e197e…` | 18岁、**U21预备队**，赛季总结却颁发"**中超联赛 助攻王**" | **Bug E：荣誉赛事名不跟随梯队** |
| 6 | `2026-08-29 181307` | Windows 智能应用控制拦截 exe（无法验证发布者） | **问题 F：无签名 exe 被拦截**（分发问题，非代码缺陷） |
| 7 | `2026-08-18 210328` | 旧版标题界面（翡翠绿按钮） | 已解决：现行 CSS 已是 V2.0 Volt 荧光绿（`--volt:#b8f535`） |
| 8 | `2026-08-18 210432` | 旧版赛季开始页（窗口过窄时侧栏被裁切） | 已解决：V2.0 布局重做；极端窄窗下的侧栏裁切仍存在，见"遗留建议" |

---

## 二、根因分析（代码级）

1. **Bug A**：事件文案中 9 处 `consequence:'…${p.name}…'` 使用**单引号字符串**而非模板字符串（`build/revamp/js4b.js` 行 65/93/114/115/123/188/202/272/288），`${}` 不会求值，渲染时原样输出。渲染点 `js3.js showConsequence` 对字符串形态不做任何处理。
2. **Bug B**：赛季事件列表在**赛季开始时一次性生成**（`js3.js generateSeasonEvents`），`condition`（如 m16/m17 要求 `['CSL','J1','K1','SAU','QAT']`）只在生成时校验。赛季中转会窗（槽位 5）转会后，后续槽位的洲际事件仍按旧联赛照常触发 → 里昂踢"亚冠"。
3. **Bug C**：通用事件 `m1`（青年联赛第8轮）与 `x11`（青年联赛双子星）没有任何梯队/年龄上下文守卫，文案硬编码"青年联赛"，任何年龄、任何梯队都会触发。
4. **Bug D**：特殊事件 `x3`（阿贾克斯球探）条件仅为 `p.ovr>=56`，未限定"仍在中国联赛"，留洋后照样触发"带你踏上欧洲舞台"的矛盾剧情。
5. **Bug E**：赛季结算荣誉直接使用成年联赛名 `LEAGUES[p.league].name`（`js5.js showSeasonEnd`），U17/U19/U21 梯队球员的赛季数据被套上"中超联赛/法甲联赛"等赛事名。
6. **附带发现**：页头球队信息在"队名=城市名"时重复显示（截图2 中"里昂 · 法甲联赛 · 里昂"）；`ch3_acl_debut` 选项文案对日韩/西亚球员写死"打出中超的传控"；构建脚本 `assemble.py / check_syntax.py / audit_refs.py` 硬编码旧机器路径 `C:\Users\Administrator\Desktop\CFC1.0`，在本机无法运行。

---

## 三、修复内容

全部改动位于源码 `build/revamp/`，随后用 `assemble.py` 重新组装进根目录 `football-career-simulator.html`（620,002 字节，自动备份 `.v1.bak`）。

| 修复 | 文件:行 | 方案 |
|------|---------|------|
| A 占位符插值 | `js3.js:251,299` | 新增 `interpolateEventText()` 运行时兜底：字符串含 `${` 时以 `new Function('p','ctx',…)` 安全求值（支持 `p`、`ctx` 及 `NT_COACH`/`starLine()` 等全局；非法表达式原样回退）；`showConsequence` 与 `showEvent` narrative 统一走该函数，一次覆盖全部 9 处存量文案及未来新增文案 |
| B 触发时二次校验 | `js3.js:177`（新增 `eventPassesNow`）、`js5.js:124`（`nextStep` 失效事件用**同类合格事件顶替**，凑不出则跳过）、`js5.js:147`（`showEvent` 入口兜底，覆盖读档恢复路径）、`js4b.js:262`（`ch6_acl_title` 补亚洲联赛条件） | 事件显示前按当前联赛/梯队/年龄/旗帜重新校验，杜绝"里昂踢亚冠" |
| C 青年赛事自适应 | `js4a.js:84`（m1）、`js4a.js:329`（x11） | 文案按 `isYouthStage(p)` 自适应：梯队显示"青年联赛/U17/U19/U21 青年队"，一线队及以上显示"联赛/主场" |
| D 球探事件限定留洋前 | `js4a.js:285` | `x3` 条件追加 `isChinaLeague(p)`；留洋后不再触发 |
| E 荣誉赛事名跟随梯队 | `js1.js:278`（新增 `competitionName(p)`：u17→U17联赛、u19→U19联赛、u21→U21联赛，其余用成年联赛名）、`js5.js:420`（金靴/助攻王/最佳阵容/最佳球员/最佳门将全部改用）、`js5.js:438`（中国金球奖限定一线队及以上） | 梯队球员荣誉不再冠以成年联赛名 |
| 附带修复 | `js5.js:635`（页头城市名与队名相同时不再重复）、`js4b.js:108`（"打出中超的传控"→"打出中国球员的传控"） | 文案一致性 |
| 工程修复 | `assemble.py`、`check_syntax.py`、`audit_refs.py` 路径改为按脚本自身位置推导；README 新增"常见问题"章节说明智能应用控制拦截 exe 的三种解决办法 | 本机可构建；分发问题有据可查 |

---

## 四、验证结果

### 第三轮验证
1. 语法审计 `SYNTAX OK`；引用审计无缺失引用。
2. **运行时冒烟测试 52/52 通过**（新增 11 项）：荣誉标准四例（19岁6球9助→最佳年轻球员、26岁6球9助→最佳阵容、助攻型MVP、金靴优先级、低产出无荣誉）、`cal_*` 年份闸门（2030 池中不再出现 2034 世预赛）、身价表现系数（爆发>基准>疲软且有界）、主题切换与持久化、教练模式全流程（高声望起点→事件→抉择→结算→升迁）。
3. **浏览器端到端**：新版赛季开始页截图（海港盾徽方正、CSL 字标比例正常、无 VS 对垒）；浅色主题切换即时生效、刷新持久、主按钮渐变/标志底板 DOM 计算样式验证通过；CSS 探针确认独立 crest 类 object-fit=contain；全程 0 个 JS 错误。

### 第二轮验证（回顾）
1. **语法审计**：`SYNTAX OK`（488,906 字符 JS）；引用审计 350 个符号无缺失引用。
2. **运行时冒烟测试 41/41 通过**（新增 16 项）：
   - G：中国联赛=季前冬窗+季中夏窗、欧洲=季前夏窗+季中冬窗、赛季首项为季前窗、双窗+结算结构、nextStep 正确传递窗期信息；
   - I：m1 选择已带 risk/fail；强制随机失败 → 无进球、`lastRiskFail=true`、状态下降；读档恢复走同一失误分支；成功分支进球+1；世预赛任意球失误 → `wc_qualified` 旗标不置位；
   - H：`ajax_interest` 旗标 → 转会市场出现荷甲/葡超报价（对照组无旗标不出）；`recovery_pro` → 衰退 -1 缓解；
   - J：39 岁结束赛季 → 40 岁强制进入退役流程；40 岁"继续征战"被拒绝。
3. **浏览器端到端**（真实点击 + 截图核验）：
   - 赛季开始 VS 页截图：海港盾徽方正完整、CSL 联赛字标在宽幅框中比例正常（对照用户 21:59 截图的压扁形态）；
   - 转会窗界面实际显示「转会窗 · 冬窗（季前 · 1月1日 — 2月28日）」；
   - 强制随机失败实测：m1「推射远角」→ 结果标题「**剧情 · 失误**」+ 专属失误文案 + 无进球 tag；
   - 全程 0 个 JS 错误。

### 第一轮验证（回顾）
1. **语法审计**：`check_syntax.py`（esprima）→ `SYNTAX OK`。
2. **引用审计**：`audit_refs.py` → 无缺失引用（`Function`/`var` 为脚本对 `new Function` 与 CSS `var()` 的已知误报）。
3. **运行时单元冒烟测试 25/25 通过**：占位符插值、触发时校验（m16 亚冠/x3 球探/年龄门槛）、`nextStep` 顶替与跳过、`competitionName` 五组合。
4. **浏览器端到端**：建档→整赛季→结算 0 错误；U21 高助攻 →「U21联赛 助攻王」；跨赛季与五页渲染正常。

---

## 五、遗留事项与建议

1. **exe 已重打包**：PyInstaller 6.22.2 重打包并覆盖根目录 `CFC足球职业生涯模拟器.exe`（28.3MB，2026-08-30 20:44，内嵌第三轮全部修复）。
2. **智能应用控制（问题 F）**：无签名分发固有问题，已在 README 提供用户侧解决办法；正式发行建议购买代码签名证书。
3. **窄窗口侧栏裁切**（旧截图 8 边缘情况）：1280px 以下右栏可能被裁切，建议后续做响应式换行。
4. **存档兼容性**：旧存档读档后事件经触发时校验自动顶替/跳过；旧存档中已存在的单转会窗事件按"季中窗"标签显示，下一赛季起自动升级为双窗结构，无需删档。
5. **风险机制调参**：`risk.base` 集中在 0.45–0.72，属性差可带来 ±20% 左右波动；若实测挫败感过强/过弱，直接调整各事件 `risk.base` 即可。

